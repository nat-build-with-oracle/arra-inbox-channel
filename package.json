{
  "name": "inbox-channel",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun --env-file=/dev/null run server.ts",
    "test": "bun --env-file=/dev/null run test-e2e.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0"
  }
}
